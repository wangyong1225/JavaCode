<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd">

    <!-- 告诉Spring创建对象
         声明bean，就是告诉Spring要创建某个类的对象
         id：对象的自定义名称，唯一值，Spring通过这个名称找到对象
         class：类的全限定名称（不能是接口，因为Spring是反射机制创建对象，必须使用类）

         spring 就完成 SomeService someService = newSomeService();
         Spring就是把创建好的对象放入到map中，Spring框架有一个map存放对象的
            springMap.put(id的值， 对象)
            列如 springMap.put("someService", new SomeServiceImpl());

         一个bean标签声明一个对象
    -->
    <bean id="someService" class="com.wy.service.impl.SomeServiceImpl" />

    <bean id="someService1" class="com.wy.service.impl.SomeServiceImpl" />


</beans>

<!--
    spring的配置文件
    1、beans：是根标签，Spring把对象称为beans
    2、spring-beans.xsd 是约束文件，和mybatis指定dtd是一样的
-->